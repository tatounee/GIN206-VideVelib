[project]
name = "VideVelib"
version = "0.1.0"
description = "Simulation IoT pour observer les stations de Velib"
requires-python = ">=3.13"
dependencies = [
    "influxdb-client>=1.50.0",
    "msgpack>=1.1.2",
    "paho-mqtt>=2.1.0",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]
